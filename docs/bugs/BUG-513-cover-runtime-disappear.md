## BUG-513 · 封面运行期探测竞态消失重启恢复
- **报告**：2026-07-01（用户：TODO-1081）
- **真实性**：✅ 真 bug（机制A）。根因 `hibiki/lib/src/media/sources/reader_hibiki_source.dart` 旧 `_resolveCoverUrl`（原 312-338）——EPUB 封面 `imageUrl` 非持久列，每次书架重建都用异步 `File(path).exists()` 即时探测；运行期 `hibikiBooksProvider` 被大量 `ref.invalidate` 重跑（reader_hibiki_source.dart:134,209；reader_hibiki_history_page.dart / reader_history/books.part.dart 多处），`deleteBook`（reader_hibiki_source.dart:416-417）删行后跑 `VACUUM`+`wal_checkpoint(TRUNCATE)`，其调用方 await 完删除后立即 `invalidate(hibikiBooksProvider)` 对其余书重探；IO 竞争下 `File.exists()` 偶发 false → 当次 `imageUrl=null` 被书架 `AsyncValue` 缓存 → 封面消失。生命周期 resume 不 invalidate 书架（main.dart 只刷主题色），故须冷启动或显式操作才重探恢复。有声书/字幕书卡封面 fallback 绑同一探测结果（`reader_history/books.part.dart:79-93` 的 `_epubCoverUrisByBookKey` 由 epub `MediaItem.imageUrl` 构建）故一并丢。
  - 与 TODO-1034 不同源：1034=导入竞态把 null 永久写 DB；1081=运行期封面消失、DB/磁盘都还在、冷启动重探即恢复。
  - 机制B（数据根迁移视频封面路径分叉，仅用户改过数据存储位置才触发）不在本档范围，由 owner 待用户确认后另行处置。
- **[x] ① 已修复** — commit `8e4a07b3e`。`hibiki/lib/src/media/sources/reader_hibiki_source.dart`：拆出纯函数 `resolveCoverUrlFor`（可注入 probe + cache）与 `coverCandidatePaths`，新增进程内 `_lastGoodCoverUrlByBookKey`（按稳定 `bookKey` 存上次成功解析的封面 URL）。探测**成功**写缓存并返回；一次瞬时**全落空**时回落缓存里的 last-good 值而非无条件 null——文件仍在盘上时封面不再被瞬时 `File.exists()` false 带空。缓存只存曾真实存在过的 URL，绝不捏造路径；空 `bookKey` 不进缓存、不回落。封面正常存在时行为零变化（never-break）。`deleteBook` 的 VACUUM/checkpoint 与调用方 invalidate 已是串行（调用方 await 完 deleteBook 再 invalidate），且 last-good 缓存已完全覆盖该竞态窗口，故未再改 deleteBook 顺序（避免低价值改动）。
- **[x] ② 已加自动化测试** — `hibiki/test/media/sources/reader_hibiki_cover_cache_test.dart`（8 例全绿）。纯函数层：探测命中写缓存 / 声明封面优先兜底 / **瞬时全落空回落 last-good 而非 null**（锁死根因不变量）/ 从未成功过且落空返回 null（不捏造）/ 空 bookKey 不串味；候选顺序守卫。真实解析链层：走 `mediaItemForBookKey` → `_bookToMediaItem` → `_resolveCoverUrl`（与书架 `getBooksFromDb` 同链），封面在盘上→`imageUrl` 解析成功；删文件模拟瞬时探测落空后重建→`imageUrl` 仍回落 last-good 不塌成 null。
- **备注**：`flutter analyze` 无参 No issues found；相关 `flutter test` 全绿。仅修机制A；机制B 待 owner 与用户确认数据位置是否变更后另处。
