## BUG-446 · 添加音频数据库失败文案无信息（吞异常）
- **报告**：2026-06-28（用户：）
- **真实性**：真 bug。错误文案 = i18n key `local_audio_import_failed`（通用「Failed to import audio database」），snackbar 在 `hibiki/lib/src/pages/implementations/dictionary_settings_dialog_page.dart:329`。核心缺陷=该处 `catch (_)` 整个丢弃异常对象，吞掉真实抛点：`hibiki/lib/src/settings/settings_schema_lookup.dart:66` 的 `result?.files.single.path`（`.single` 在 0/多文件时抛 StateError）、`FilePicker.pickFiles()` 的 PlatformException、以及 `hibiki/lib/src/models/local_audio_manager.dart:150` `sourceFile.copy()` 的 FileSystemException（磁盘满/无写权限/目录不存在）。另有假成功隐患：`local_audio_manager.dart:149` 源文件不存在时静默跳过 copy，返回指向空 path 的 entry。
- **[x] ① 已根因修复** — 三处：①`dictionary_settings_dialog_page.dart` `_addLocalDb` 的 `catch (_)` 改 `catch (e, st)`，调 `ErrorLogService.instance.log('AudioSourcesDialog.addLocalDb', e, st)` 记完整诊断，snackbar 改用新 i18n key `local_audio_import_failed_detail(reason: '$e')` 把异常摘要带进可见反馈。②`settings_schema_lookup.dart` 的 `onPickLocalDb` 去掉会抛 StateError 的 `files.single`，改 `files.first`，文件数异常/path 为空时记一条 `AudioSourcesDialog.pickLocalDb` 诊断日志（含 count 与 pathNull），path 为空时显式 `throw Exception` 交上层弹反馈。③`local_audio_manager.dart` `importFile` 源文件不存在改 `throw FileSystemException`（消除假成功），copy 失败包 try 把 `FileSystemException.osError`（真 errno）带出重抛。i18n 新 key 经 `dart run tool/i18n_sync.dart --add local_audio_import_failed_detail`（17 文件齐 + `dart run slang` 重生成）。提交：见本轮 commit。
- **[x] ② 已加自动化测试** — ①`hibiki/test/models/local_audio_manager_test.dart` 新增 `importFile throws when the source file does not exist (BUG-446)`（断言抛 FileSystemException + 库目录无空副本残留）。②`hibiki/test/pages/audio_db_import_error_guard_test.dart` 新建源码扫描守卫：断言裸吞异常旧路径已移除、dialog 带 `catch (e, st)` + ErrorLogService.log + 带 reason 文案、picker 不再用 `files.single` 且记 count 日志、importFile 显式 throw（防回归）。
- **备注**：用户已明确授权「增加报错日志」。成功路径行为未变，仅增日志 + 暴露已发生异常 + 修假成功。
