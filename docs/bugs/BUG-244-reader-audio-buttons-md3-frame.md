## BUG-244 · 阅读器有声书音频控制键被改成扁平样式，需还原「图标 + 圆框 md3」旧观感（TODO-297）
- **报告**：2026-06-14（用户：把阅读器音频键改回去之前的版本，图标和圆框 md3）。这是一次 UI 还原诉求，非功能性 bug；按仓库「一 bug 一文件」习惯登记。
- **现状**：`hibiki/lib/src/media/audiobook/audiobook_play_bar.dart` 的 `AudiobookPlayBar`（挂载于 `reader_hibiki_page.dart:6000`）所有按钮都用自定义 `HibikiIconButton`（`InkWell` 圆形 + 可选 `backgroundColor`），观感扁平；只有播放键带一层半透明 tonal 底，没有标准 MD3 的 filled-tonal 圆框 + state-layer + ripple。
- **根因**：提交 `48a8d2044`（refactor(reader): share audiobook icon controls）把原生按钮统一换成 `HibikiIconButton`。在此之前（`48a8d2044~1`），播放/暂停键是原生 `IconButton.filledTonal`（标准 MD3 圆形 tonal 容器框），其余键（上一句/下一句/follow/设置）是无框原生 `IconButton`。该重构同时被源码守卫 `test/media/audiobook/reader_quick_settings_sheet_static_test.dart` 的「audiobook play bar uses shared MD3 icon controls」用例锁死，所以单纯改源码会让该守卫翻红——属于代际守卫，需要随设计回退一并翻转。
- **[x] ① 已修复**：把播放/暂停键从 `HibikiIconButton` 还原为原生 `IconButton.filledTonal`（恢复标准 MD3 圆框 + state-layer + ripple）；上一句/下一句/follow/设置键还原为无框原生 `IconButton`。图标沿用旧版 outlined 变体（`skip_previous_outlined`/`fast_rewind_outlined`/`pause_outlined`/`play_arrow_outlined`/`skip_next_outlined`/`fast_forward_outlined`/`link`/`link_off`/`tune_outlined`）。**保留 48a8d2044 之后的全部功能改进**：纸张主题前景色注入（`c3dbe59a1`，现经 `IconButton.styleFrom` 的 `foregroundColor` / 12% tonal `backgroundColor` 注入）、`reversed` 镜像 + ⏮⏯⏭ 原子组防方向颠倒（`b28c492c8`/BUG-021）、设置键文案 Reader Settings（`eec7f4193`，`t.reader_settings_section`）。构造签名不变，未改 `reader_hibiki_page.dart`。
- **[x] ② 已加自动化测试**：`hibiki/test/media/audiobook/audiobook_play_bar_md3_frame_test.dart`（widget 行为守卫）断言播放/暂停键渲染出非透明背景容器（filled-tonal 圆框）、其余键背景透明，并验证注入纸张前景色后播放键仍保持 tonal 圆框；同时把陈腐的源码守卫 `reader_quick_settings_sheet_static_test.dart` 的「audiobook play bar ...」用例翻转为「restores the MD3 filled-tonal play frame (TODO-297)」，锁定 `IconButton.filledTonal(` 存在且不再含 `HibikiIconButton(`。`flutter test test/media/audiobook/` 全绿（370 passed）。
- **备注**：「忠实还原旧版」= 播放键圆框（filledTonal）、其余键无框。若用户其实想要**所有键**都加圆框，那是从未存在过的新设计（旧版 prev/next/follow/settings 一直无框），需另行确认后再扩展。真机视觉复测待用户。
