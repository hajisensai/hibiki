## BUG-506 · TODO-1059 菜单播放按钮时自动隐藏误触
- **报告**：2026-07-01（用户：）
- **真实性**：✅ 真 bug。根因：vendored fork `third_party/media_kit_video/lib/media_kit_video_controls/src/controls/material.dart:563` 的自动隐藏 `_timer`（`controlsHoverDuration`，Hibiki 设 2s）**只**在整屏 `onTap`（:742）与 seek 时重置；移动端底部按钮栏 play/快进/退各自的 `onPressed` 不重置它。Hibiki 唯一续命入口 `_pokeControlsVisible`（`video_hibiki/controls_visibility.part.dart:29`）开头 `if(!_isDesktopVideoControls)return` 移动端整体早退 → 所有续命 no-op。结果：用户还在按底部按钮，控制条 2s 到点即隐藏，手指落到按钮下方画面 → 误触 play/pause。
- **[x] ① 已修复** — ①fork `MaterialVideoControlsThemeData` 加 `Listenable? restartHideTimerSignal` 字段（构造器/copyWith 透传），控制条 State 在 `didChangeDependencies` addListener、`dispose` removeListener，新增 `_restartHideTimer()`（仅可见态 cancel+重排隐藏 Timer，隐藏态早退不 un-hide）。②Hibiki 定义 `_RestartHideTimerSignal extends ChangeNotifier{poke()=>notifyListeners()}`（`video_hibiki_page.dart`，随 State dispose）。③`_pokeControlsVisible` 门控检查提前到平台无关处，移动分支改 `_restartHideTimerSignal.poke(); return`，桌面仍派合成 hover。④`_mobileControlsTheme` 传 `restartHideTimerSignal: _restartHideTimerSignal` 入 fork theme（端到端接线）。补：play/pause 按钮（`_activateVideoControlItem` playPause + `MaterialPlayOrPauseButton` 外套 `Listener(onPointerDown)`）也 poke（seek/上下句本已 poke）。PATCHES.md 已记录。提交：（见分支 fix-1059-subtitle-controls-v2）
- **[x] ② 已加自动化测试** — `hibiki/test/third_party/media_kit_video_restart_hide_timer_guard_test.dart`：fake_async 行为层（忠实复刻 fork `_restartHideTimer` 计时契约，真实 ChangeNotifier 驱动，断言窗口内反复 poke 跨越 controlsHoverDuration 仍 visible、停手后正常隐藏、隐藏态 poke no-op）+ 源码守卫（fork 字段/订阅/重排方法 + Hibiki signal 定义/poke 早于门控/传入 fork theme 端到端）。fork 真实控制条挂载需 libmpv、headless 跑不了，故行为层用抽出的同构计时逻辑 + 源码守卫锁 fork 结构一致，两层防回归。
- **备注**：无 i18n。
