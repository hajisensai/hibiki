## BUG-509 · TODO-1065 悬浮字幕首句空窗 / 每句要等上一句播完才出现
- **报告**：2026-07-01（用户：）
- **真实性**：✅ 真 bug。根因 `packages/hibiki_audio/lib/src/audiobook/audiobook_controller.dart:950`（`_updateCurrentCue` 在 `JsonAlignmentParser.findCueIndex` 返回 `idx<0` 时**裸 return 不更新** `_currentCue`）。`findCueIndex`（`packages/hibiki_audio/lib/src/parsers/json_alignment_parser.dart:102-130`，闭区间 `[startMs,endMs]`）在①位置早于首句 startMs、②落在句间静音 gap 两种情况返回 -1。裸 return 是 reader 正文高亮**刻意的 gap-hold 契约**（避免高亮闪烁，见 findCueIndex 闭区间注释 + BUG-074 家族）。但悬浮字幕窗、书架 mini bar 字幕行、锁屏/媒体通知副标题**四表面共用同一** `AudiobookPlayerController.currentCue`（125ms tick 驱动，`_startPositionTracking` `audiobook_controller.dart:863-875`）。喂文本路径 `hibiki/lib/src/media/audiobook/audiobook_session.dart:294-319` `_syncFloatingLyric` 读裸 `controller.currentCue`。于是：①音频开头到首句 startMs 之间 `_currentCue` 停在初始 null → 悬浮字幕 `updateText('')` 全空直到首句发声（首句空窗）；②相邻句静音 gap 期间 hold 上一句，直到下一句 startMs 才切（等上一句播完才出现下一句）。
- **[x] ① 已修复** — commit 见下。**不改** `_updateCurrentCue` 的 `idx<0` 裸 return 语义（守 reader 高亮 hold 契约不回归）。改为在显示层隔离：`AudiobookPlayerController` 新增只读 getter `AudioCue? get displayCueForFloatingLyric`（`audiobook_controller.dart`），在 `effectiveMs = pos - delayMs` 空间独立重算：命中 cue 区间(`idx>=0`)→当前句；早于首句→首句(index 0)；落 gap→下一条 startMs 严格大于当前位置的 cue；晚于末句→末句（不清空）。配套 `int displayCueIndexIn(List<AudioCue> cues)` 把 display cue 映射回选定列表下标供 N>0 上下文窗口用。`_syncFloatingLyric`（N=0 与 N>0 两分支）、锁屏通知副标题 `_syncMediaNotification`、书架 mini bar `now_listening_mini_bar.dart` 三表面改用 display getter。**reader 正文高亮 / 歌词跟随（`onReaderCueChanged` → reader parts 读 `currentCue`）继续用原 `currentCue`，不受影响。** 不引入延迟/定时器 hack，不破坏 delayMs 同步。改动文件：`packages/hibiki_audio/lib/src/audiobook/audiobook_controller.dart`、`hibiki/lib/src/media/audiobook/audiobook_session.dart`、`hibiki/lib/src/media/audiobook/now_listening_mini_bar.dart`。
- **[x] ② 已加自动化测试** — 单测 `packages/hibiki_audio/test/audiobook/display_cue_for_floating_lyric_test.dart`：构造 cues + 一组 position（首句前 / 首句内 / 句间 gap / 末句后），断言 `displayCueForFloatingLyric` 在首句前返回首句、gap 内返回下一句、句内返回当前句、末句后返回末句；并断言 `currentCue`（reader 高亮用）在 gap/首句前仍保持原 `idx<0` hold 语义**不变**（守卫 reader 契约不回归）。
- **备注**：BUG 号手动取 509（tool 自动分配撞并发占号，若集成落地时仍冲突由 integration owner 重编）。native-assets（sqlite3 DLL）在本机走代理 TLS 握手失败，仅阻塞 DB 相关测试；本 getter 单测走 fake player、不触 DB，不受影响。full `flutter analyze` = No issues。
