## BUG-507 · TODO-1065 悬浮字幕查词弹窗<html>不透明泛白
- **报告**：2026-07-01（用户：）
- **真实性**：✅ 真 bug。根因 `hibiki/assets/popup/popup.css:20-39`（`html,body{background-color:var(--background-color,transparent)}` 把 `<html>` documentElement 铺满不透明主题 surface）+ `hibiki/lib/src/pages/implementations/popup_settings_injection.dart:64-66`（旧 classLine 只对桌面 global-lookup 加透明标记，悬浮字幕/外部弹窗路径无任何中和 class）。悬浮字幕点字查词经 `popup_main.dart → PopupDictionaryPage → DictionaryPopupLayer → DictionaryPopupWebView`，该弹窗跑在透明浮动窗里、圆角卡由 Flutter `HibikiPopupSurface` 画；`<html>` 近白填充铺满整个 WebView 视口盖在 Flutter 卡片之上，浅色主题读成整窗泛白。桌面 global-lookup 早有 `html.global-lookup{background:transparent}`（popup.css:58-60）中和，移动/外部路径漏了。
- **[x] ① 已修复** — commit 见下。新增 `mobile-external` 文档 class（`popup.css` `html.mobile-external{background:transparent}`，只透明 `<html>`、不加 body 卡片 chrome，卡片由 Flutter surface 画）。经 `PopupSettingsOptions.mobileExternal` 从单一真相源 `buildPopupSettingsJs` 注入该 class（与 global-lookup 同构、互斥分支）；`DictionaryPopupWebView.transparentDocumentBackground`（默认 false=原 in-app 行为）→ `DictionaryPopupLayer` 转发 → 仅 `popup_dictionary_page.dart` 悬浮字幕/外部弹窗宿主置 true。in-app（reader/video/home）与桌面 global-lookup 不受影响（零回归）。改动文件：`hibiki/assets/popup/popup.css`、`hibiki/lib/src/pages/implementations/popup_settings_injection.dart`、`hibiki/lib/src/pages/implementations/dictionary_popup_webview.dart`、`hibiki/lib/src/pages/implementations/dictionary_popup_layer.dart`、`hibiki/lib/src/pages/implementations/popup_dictionary_page.dart`。
- **[x] ② 已加自动化测试** — 源码守卫 `hibiki/test/lookup/mobile_external_popup_washout_guard_test.dart`（10 用例全绿）：断言 popup.css `html.mobile-external` 透明且不画 body chrome、in-app opaque 填充保留、shared builder 注入 `mobile-external` class（与 global-lookup 互斥）、widget 三层 wiring（WebView/Layer 默认 false + popup_dictionary_page 置 true + in-app 两宿主永不置位）。参照 `global_lookup_popup_style_guard_test.dart` / `global_lookup_inapp_isolation_guard_test.dart`（它们只覆盖桌面=本 bug 漏测处）扩展覆盖移动外部弹窗路径。
- **备注**：只修泛白；「电脑悬浮字幕查词点击无反应 / 首句要等上一句播完」是 TODO-1065 的另外子问题，本次未复现同源、未处理。native-assets（sqlite3 DLL）在本机走代理 TLS 握手失败，仅阻塞 DB 相关测试；本守卫为纯源码读取，不受影响。full `flutter analyze` = No issues。
