## BUG-242 · 制卡「添加来源分类标签」开关提示把视频写成 anime/动漫
- **报告**：2026-06-13（用户/TODO-295：添加来源分类标签，里面视频的标签要改名，不应该是动画标签）
- **真实性**：✅ 真 bug（用户可见文案与实际行为不一致）。Anki 设置页「添加来源分类标签」开关的副标题提示（i18n key `anki_tag_include_category_hint`，渲染于 `hibiki/lib/src/pages/implementations/anki_settings_page.dart:155-156`）在 17 种语言里都写「视频标 anime」（zh-CN：`书籍标「book」、视频标「anime」`；其余 16 文件：`Books get "book", videos get "anime"`），但实际写进 Anki 卡片的视频分类标签字面量早已是 `video`（`packages/hibiki_anki/lib/src/base_anki_repository.dart:109` `videoTag = 'video'`，`_categoryTagForSource` 把 `AnkiMiningSource.video` 映射成 `videoTag`）。即提示文案宣称视频卡片得到 `anime` 标签，与真实写入的 `video` 标签矛盾，误导用户。`anki_models.dart:273,283`、`dictionary_page_mixin.dart:98`、`video_hibiki_page.dart:2469` 的注释也残留「视频/动漫→anime」旧描述。
- **[x] ① 已修复** — 提交 待提交。① 用户可见文案：`anki_tag_include_category_hint` 在全 17 个 i18n 源文件里把 `anime`/`动漫` 改成 `video`（zh-CN：`书籍标「book」、视频标「video」`；其余：`Books get "book", videos get "video"`），值编辑不动 key 集合（遵守 i18n 纪律，未新增/删除 key），`dart run slang` 重新生成 `strings.g.dart`。② 注释一致性：`packages/hibiki_anki/lib/src/anki_models.dart:273,283`、`hibiki/lib/src/pages/implementations/dictionary_page_mixin.dart:98`、`hibiki/lib/src/pages/implementations/video_hibiki_page.dart:2469` 的「动漫/anime」陈旧注释统一改为「视频/`video`」。**未改实际写入的 tag 字面量**——它早已是 `video`，无任何 `anime` 残留写入路径，故无旧卡迁移问题。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/mining_default_tags_wiring_static_test.dart` 新增守卫「分类标签提示文案描述视频标签为 video，而非 anime/动漫」：断言 `AppLocale.en` / `AppLocale.zhCn` 的 `anki_tag_include_category_hint` 含 `video`、不含 `anime`/`动漫`（读生成的 `strings.g.dart`，即 app 真实显示文案）。实际写入的 tag 为 `video` 由既有行为测试 `packages/hibiki_anki/test/mining_tag_and_parallel_test.dart` 咬住（`video source -> ['hibiki','video']`，两后端对称）。验证：`flutter test test/pages/mining_default_tags_wiring_static_test.dart test/i18n` 15 绿、`flutter test test/anki` 53 绿、`packages/hibiki_anki` mining 测试 17 绿；`flutter analyze` 仅余 slang 既有的 `scriptCode` unused warning（与本改动无关，develop 已存在）。
- **备注**：纯文案/注释改动，无运行时行为变化（视频卡片的实际标签 `video` 不变），不涉及旧 anime 卡片迁移，无需真机复测。用户 Anki 库里若存在更早版本（曾写 `anime`）制出的历史卡片，其 `anime` 标签是历史数据，按 `base_anki_repository.dart:107-108` 注释约定**不迁移/不重写旧卡**——这是既有产品策略，本次未触碰。
