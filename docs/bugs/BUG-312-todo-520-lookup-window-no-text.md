## BUG-312 · TODO-520: 0.9.24-debug.5191 查词窗口没文字
- **报告**：2026-06-18（用户：`查词窗口没文字了0.9.24-debug.5191`；版本：`v0.9.24-debug.5191+0c18e98`）
- **真实性**：✅ 真 bug。根因在 `hibiki/lib/src/pages/implementations/dictionary_popup_webview.dart:475` 附近：修复前 `_pushResults()` 在 `widget.result.entries.isEmpty` 时直接 `return`，导致 warm / 复用的 `DictionaryPopupWebView` 收到空词条结果或汉字-only 结果时不会注入 `window.lookupEntries` / `window.kanjiResults`，也不会调用 `window.renderPopup()`。`popup.js` 本身已经在 `hibiki/assets/popup/popup.js:2222` 后实现 no-results 和 kanji-only 渲染，并会发 `popupRendered`；宿主层提前返回会让阅读器/视频弹窗显示空白或残留 WebView，而不是搜索词、释义或明确“无结果”占位。
- **[x] ① 已修复** — 移除 `_pushResults()` 的空 entries 早退，让空结果和汉字-only 结果也走同一条 JS 注入与 `renderPopup()` 路径；同时注入本地化 `window._noResultsMessage`，使 WebView 分支与 Flutter 占位文案一致（`dictionary_popup_webview.dart:569`）。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/dictionary_popup_webview_test.dart` 新增静态契约，断言空结果/汉字-only 结果不会被 Dart 层早退拦截，且 `lookupEntries`、`kanjiResults`、`_noResultsMessage` 会在 `renderPopup()` 前注入。
- **备注**：已沿真实路径复核首页词典 `_searchGeneration` / controller text / result 清理、阅读器 `reader_hibiki_page.dart` 5191 native gamepad key event 路由、`BaseSourcePage.searchDictionaryResult` warm popup 复用、桌面 `DesktopLookupService` pending 消费，以及 Android 外部 `PopupDictionaryPage` 兜底入口。编号冲突处理：TODO-518 已占用 `BUG-311`，本任务改用 `docs/bugs/BUG-312-todo-520-lookup-window-no-text.md`，并已跑 `dart run tool/bug.dart reindex` 更新 `docs/BUGS.md`。
- **验证**：证据目录：`.codex-test/todo-520-lookup-window-no-text/`。已通过：`powershell -ExecutionPolicy Bypass -File tool/setup_worktree.ps1`（临时补 Git Bash PATH）、`dart format .`（格式化检查通过，非本任务格式漂移已剥离）、`flutter test test/pages/home_dictionary_pull_preserve_query_test.dart test/pages/dictionary_popup_webview_test.dart test/pages/dictionary_popup_eager_mount_guard_test.dart test/pages/popup_layer_loading_cover_guard_test.dart test/pages/dictionary_page_mixin_warm_slot_test.dart`、桌面 pending/剪贴板链路 `home_dictionary_pending_on_mount_test.dart desktop_lookup_to_dictionary_tab_test.dart desktop_lookup_service_test.dart`、阅读器/base popup 链路 `base_source_page_warm_popup_test.dart base_source_page_nested_popup_flash_test.dart base_source_page_hot_popup_test.dart reader_popup_caret_static_test.dart reader_popup_focus_static_test.dart`、`node test/utils/misc/popup_asset_behavior_test.js`、外部 popup 兜底 `popup_dictionary_page_test.dart popup_dictionary_page_nested_test.dart native_popup_dictionary_static_test.dart`。
- **设备/集成限制**：Android `bash ci/integration-test.sh --only=popup_dictionary,reader_popup_caret,reader_dictionary` 因 AVD `hoshi_test_api35` 240 秒未上线失败，`adb devices` 无设备；Windows `tool/run_windows_itest.ps1 integration_test/popup_dictionary_test.dart` 未进入测试体，CMake 阶段反复因 `media_kit_libs_windows_video` 可再生下载包完整性失败（`mpv-dev...7z` 后为 0 字节 `ANGLE.7z`）中断。完整日志保留在 `.codex-test/todo-520-lookup-window-no-text/` 和 `hibiki/.codex-test/windows-itest/win-itest-20260618-210602-1f5f599b/`。
