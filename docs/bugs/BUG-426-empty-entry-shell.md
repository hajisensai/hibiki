## BUG-426 · 隐藏词典致空正文壳卡（TODO-833）
- **报告**：2026-06-25（用户：）
- **真实性**：✅ 真 bug。查「おおかた」弹窗出两张同标题同徽章卡：第一张只有标题+频率徽章（JPDBv2/15199）正文空白，第二张同标题同徽章+完整明鏡释义。根因 `hibiki/assets/popup/popup.js:2144 buildEntryElement`：旧实现无条件先 `appendChild(createEntryHeader)`（:2146），glossary 段经 `createGlossarySectionWrapper`（:1641）在词典全被 hiddenDictionaryNames 剔除或为空时 `return null`（:1661）→ glossary 不渲染，但 header + 频率壳卡仍在 = 空壳卡。同 expression 不同 reading 拆两卡（其一空壳）。这是 BUG-419（隐藏词典过滤）下游遗留缺口（BUG-419 只剥正文没删壳卡）。
- **[x] ① 已修复** — 方案A（零 native 改动，仅 WebView popup.js）：`buildEntryElement` 增判据，glossaryWrapper === null（该词条所有词典都被隐藏或本无 glossary）→ 整卡跳过（return null），在 header 构建前就早返回，不再产生空壳。空壳卡的频率徽章冗余（同 expression 主卡已带相同徽章），删之不丢信息。`renderPopup` / `updatePopupIncremental` 跳过 null 卡，并维护 `window._entryDomIndex` 映射（每个 entries 索引 → 其 `.entry` DOM 索引，-1=跳过），使稀疏 DOM 节点与 entries 数组保持对齐——否则 incremental load-more 会用 `existingEntries[idx]` 把 A 词条释义灌进 B 卡（比原 bug 更严重）。native 弹窗 `dictionary_popup_native.dart`（移动端独立渲染路径）不在本次范围；若 native 也复现空壳卡需另开 TODO。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/popup_empty_entry_card_test.js`（Node vm 真执行 popup.js）+ `.dart` 驱动：①两 entry 同 expr 不同 reading，entry0 全 hidden+带 frequencies → renderPopup 后只一张 .entry 卡且无多余 hr；②单正常 entry → 一卡；③纯 kanji-only → kanji 卡照常、零 term 卡（不误杀）；④有正文+frequencies → freq 徽章仍显示（不退化成留壳）；④b 纯判据 buildEntryElement/entryGlossaryWrapperOrNull 全 hidden 返 null；⑤incremental 对齐：三 entry 中间被跳过后 updatePopupIncremental 把新释义灌对最后一张卡（防灌错卡）。忠实回退（恢复旧 `if(glossaryWrapper){...}` 块）后 Test1 红（expected 1 got 2）证明守得住。加 Dart 源码守卫：判据 `glossaryWrapper===null→return null` 须在 header append 前、`_entryDomIndex` 映射须在 renderPopup 存且 updatePopupIncremental 读。
- **备注**：与 TODO-804/BUG-419（隐藏词典仍显示）相邻：BUG-419 修复制造了本子因（只剥正文没删壳），本 bug 补其下游缺口，不回退 BUG-419 过滤。与 TODO-812/BUG-421（明鏡@media 作用域）无关。声明修好前真机查「おおかた」复测空卡消失、明鏡释义+频率徽章仍在（真机待用户）。
