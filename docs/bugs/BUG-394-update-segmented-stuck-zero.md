## BUG-394 · 自动更新分片下载卡0%(TODO-596回归)
- **报告**：2026-06-21（用户：）
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/utils/misc/update_checker_download.dart:247`（TODO-596 分片下载引入）：下载阶段 6 候选 URL（1 直连 + 5 镜像）在 `_downloadUpdateAssetUncoalesced` 的 candidateUrls **串行** for 循环里逐个试；走分片的大文件每候选先发探针（`Range: bytes=0-`），探针用 `_kPerAttemptTimeout`（15s）整体超时。GFW 下坏候选 TCP 连上却挂起不返首字节，每个坏候选累积吃满 15s，首字节到达前进度恒显 0%——观感像「分片下载卡 0%」。
- **[x] ① 已修复** — TODO-683 三步根因修复（不关分片，让分片可靠）。提交见报告。
  - **Step1 体感快修**：`update_checker_release.dart:_runDownloadAndInstall` 下载遮罩状态初值改 `t.update_connecting`（「正在连接更新源…」），首个非零 onProgress / onDiagnostics 信号到达再翻 `t.update_downloading`。逻辑下沉到 `UpdateDownloadStatusController`（race part，可测）。新增 i18n key `update_connecting`（17 语言齐全，经 `tool/i18n_sync.dart` + `dart run slang`）。
  - **Step2 首字节超时**：新增 `_kFirstByteTimeout = 5s`（race part），探针只用它计时（快判死「连上即挂」的坏候选）；分段正式下载各段仍用 `_kPerAttemptTimeout`（15s 不变）。
  - **Step3 候选并发竞速选源**：新建 `update_checker_race.dart` part。`raceSelectFastestCandidate` 对所有候选**并发**发探针（只探不真下载），首个返 206 + 拿到总大小者触发裁决（镜像胜出再等 500ms tie-break 看直连是否近乎同时到，直连优先）；落败 response 一律 `drain()` + try/catch 吞错（R1 取消语义，`bool settled` 单 isolate 只采首裁决）。胜出 url 经 `reorderCandidatesByRaceWinner` 提到候选列表首位（**只重排不删减**，保留串行换源能力），交回现有串行循环真下载。门控 `_shouldRaceCandidates`：`length<=1` / 单线程 / 小文件(size 已知切不出多段) / 有续传残留 → 不竞速，退化现有行为（保现有 13 个分片/续传/镜像用例零回归）。竞速选不出源 → 用原候选顺序，`selectRepresentativeDownloadFailure`（TODO-666）仍锚定直连失败。
- **[x] ② 已加自动化测试** — `hibiki/test/utils/misc/update_checker_race_test.dart`（29 用例）：竞速选最快 / tie-break 直连优先 / 落败被 drain 无 uncaught（R1 守卫）/ 首字节 ~5s 判死（fakeAsync）/ 竞速全失败仍锚定直连（端到端守 TODO-666）/ length==1 退化 / Step1 connecting→downloading widget 翻转 + 幂等。结构守卫 `update_checker_structure_guard_test.dart` 登记新 race part + 5 part 断言。i18n 完整性 + zh-CN mojibake 守卫绿。现有 `update_checker_segmented_download_test.dart` / `_download_resume_test.dart` / `_mirror_fallback_test.dart` 全过（零回归）。
- **备注**：竞速只重排候选顺序，不增删任何网络端点（`updateCheckProxyPrefixes` 是 TODO-666 刚治理过的镜像清单，不动）。Step4「首段复用」延后未做（探针 body 一律 drain，正式下载由现有 `_downloadSegmented` 重新探+取）。真机/GFW 实测待用户验证原始失败路径（无头测试已固定竞速选源、首字节超时、状态翻转的逻辑不变式）。
