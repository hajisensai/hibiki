## BUG-163 · 桌面制卡闪退：WebView2 捕获帧迟到事件打进已拆除 delegate
- **报告**：2026-06-09（飞书 TODO-013：「我电脑制卡闪退了，你修复一下」）。与 TODO-012「视频制卡整句音频/画面不动」区分：本条崩溃证据落在 Windows WebView2/GraphicsCapture 捕获帧 teardown，不是字幕/视频帧抽取或整句音频范围。
- **真实性**：✅ **真 bug（本机 WER + dump 栈确认）**。`C:\Users\wrds\AppData\Local\CrashDumps\hibiki.exe.68244.dmp`（2026-06-09 19:15:23，`hibiki.exe` 0.5.0.33）对应事件日志 `GraphicsCapture.dll` `0xc0000005`，`cdb` 栈落在 `GraphicsCapture!TypedEventHandler<Direct3D11CaptureFramePool,...>::operator()+0x15`，`rcx=0`，下方是 `Direct3D11CaptureFramePool::FirePresentEvent` 和 CoreMessaging UI 线程消息派发。根因在 `packages/flutter_inappwebview_windows/windows/custom_platform_view/texture_bridge.cc:79` 注册 FrameArrived 回调时捕获裸 `this`，且旧 `StopInternal()` 在 teardown 中先 `remove_FrameArrived` 再 `Close()` frame pool；已经排到 UI 消息循环里的 `FirePresentEvent` 仍会执行，remove 后事件源内部可命中空 `TypedEventHandler` delegate，或迟到回调碰到已释放 bridge。
- **[x] ① 已修复** — 本提交。`TextureBridge` 新增 `FrameArrivedCallbackState`，FrameArrived lambda 改为捕获 `callback_state` 而不是裸 `this`；`Stop()`/析构先 `InvalidateFrameArrivedCallback()`，把 state 置为 inactive/null，再关闭 `capture_session_` 和 `frame_pool_`。同时停止在 teardown 期 `remove_FrameArrived`，避免已排队 `FirePresentEvent` 走到空 delegate；`frame_pool_` 仍 `Close()` 并置空，切断后续事件源。
- **[x] ② 已加自动化测试** — `hibiki/test/widgets/texture_bridge_stop_guard_test.dart` 扩展源码守卫：断言存在 `FrameArrivedCallbackState` / `InvalidateFrameArrivedCallback` / `[callback_state]`，禁止 FrameArrived lambda 捕获裸 `this`，禁止 `remove_FrameArrived` 回到 teardown 路径，并保留 `pool_closable` + `frame_pool_ = nullptr`。
- **复发（TODO-021）**：2026-06-09 22:25:08 +08:00 新 dump `C:\Users\wrds\AppData\Local\CrashDumps\hibiki.exe.67804.dmp` 仍为 `GraphicsCapture.dll` `0xc0000005`，`GraphicsCapture!TypedEventHandler<Direct3D11CaptureFramePool,...>::operator()+0x15`，`rcx=0`，下方同为 `Direct3D11CaptureFramePool::FirePresentEvent`。这说明 TODO-013 的 shared callback state 只保护了“进入 Hibiki lambda 后不碰已销毁 bridge”，但没有保护 WGC 在进入 lambda 之前调用的 `TypedEventHandler` delegate 本体；`TextureBridge::Start()` 仍把 `Microsoft::WRL::Callback(...).Get()` 临时对象直接传给 `add_FrameArrived`，queued `FirePresentEvent` 可在 teardown/Close 后命中空 delegate。
- **[x] ① TODO-021 已修复** — 本轮提交。`TextureBridge` 新增成员持有的 `Microsoft::WRL::ComPtr<FrameArrivedHandler> frame_arrived_handler_`，声明在 `frame_pool_` 之前以保证成员析构逆序中 frame pool 先释放、delegate 后释放；`Start()` 先把 WRL callback 保存到成员，再用 `frame_arrived_handler_.Get()` 注册给 WGC。原有顺序保持：Stop/析构先 invalidate shared state，不恢复 `remove_FrameArrived`，再 Close session/pool 并置空 pool。
- **[x] ② TODO-021 已加自动化测试** — `hibiki/test/widgets/texture_bridge_stop_guard_test.dart` 扩展源码守卫并已红→绿：断言 `TextureBridge` 持有 `frame_arrived_handler_`、注册时使用 `frame_arrived_handler_.Get()`，禁止回退到 `add_FrameArrived(Microsoft::WRL::Callback(...).Get())` 临时 delegate，并检查 delegate 成员声明在 `frame_pool_` 前。
- **复发（TODO-031）**：2026-06-10 07:19:10 +08:00 新事件 `EventRecordID=7260958` / `Report ID=8e42b081-a733-400d-a2ee-f6e7c9e58286`，dump `C:\Users\wrds\AppData\Local\CrashDumps\hibiki.exe.73308.dmp`。`cdb` 确认仍是 `GraphicsCapture.dll` `0xc0000005`，`GraphicsCapture!TypedEventHandler<Direct3D11CaptureFramePool,...>::operator()+0x15`，`rcx=0`，下方同为 `Direct3D11CaptureFramePool::FirePresentEvent`。当前 `HEAD` 已包含 TODO-021 提交 `a0f31988e`，且 `TextureBridge` 旧防线仍在，因此本次不是旧修复未合入；新的根因是 `packages/flutter_inappwebview_windows/windows/custom_platform_view/custom_platform_view.cc:199` 的 `CustomPlatformView::~CustomPlatformView()` 先调用 `TextureRegistrar::UnregisterTexture()`，随后才等成员析构触发 `TextureBridge::~TextureBridge()` 关闭 WGC frame pool。`UnregisterTexture()`/UI 消息泵窗口内，CoreMessaging 已排队的 `FirePresentEvent` 仍可在 frame pool 未 Close 前派发，命中 WGC 内部空 handler；同时 `custom_platform_view.h` 中 `flutter_texture_` 声明在 `texture_bridge_` 前，成员析构逆序会让捕获 bridge 裸指针的 `TextureVariant` 回调对象比 bridge 活得更久，扩大 teardown 竞态窗口。
- **[x] ① TODO-031 已修复** — 本轮提交。`CustomPlatformView::~CustomPlatformView()` 在 `UnregisterTexture()` 前显式调用 `texture_bridge_->Stop()`，让 WGC capture session/frame pool 在 Flutter texture 注销和潜在消息派发前先 invalidate + Close；并调整成员声明顺序，使持有 texture 回调的 `flutter_texture_` 先于 `texture_bridge_` 析构，避免回调对象持有已销毁 bridge 的裸指针。
- **[x] ② TODO-031 已加自动化测试** — `hibiki/test/widgets/texture_bridge_stop_guard_test.dart` 扩展源码守卫并已红→绿：断言 `CustomPlatformView` 析构先 `texture_bridge_->Stop()` 再 `UnregisterTexture()`，并断言 `texture_bridge_` 声明在 `flutter_texture_` 前以获得正确成员析构逆序。
- **备注**：TODO-013 验证证据：`D:\flutter_sdk\flutter_extracted\flutter\bin\flutter.bat test test\widgets\texture_bridge_stop_guard_test.dart` 红→绿；`D:\flutter_sdk\flutter_extracted\flutter\bin\flutter.bat build windows --release` 编译通过；`dart run tool\bug.dart check` 通过；全量 `flutter test` 首轮并发跑在 `flutter_tester` WebSocket upgrade 处失败（日志 `.codex-test/flutter-test-todo-013/`），按仓库已知 fallback 用 `--concurrency=1` 复跑通过（日志 `.codex-test/flutter-test-todo-013-concurrency1/flutter_test.jsonl`）。dump 解析日志保存在 `.codex-test/todo-013-graphicscapture-68244-stack.txt`。TODO-021 新 dump 解析日志保存在 `.codex-test/todo-021-graphicscapture-67804-stack.txt`；本轮验证包含 `flutter test test\widgets\texture_bridge_stop_guard_test.dart` 红→绿、`dart run tool\bug.dart check` 通过、`flutter build windows --release` 编译通过。本轮未启动真实制卡路径，避免在未隔离用户数据/Anki 导出环境时制造额外副作用。同一天还看到另一类 `bonsoir_windows_plugin`/`FlutterDesktopMessengerSetCallback` 的退出期 dump（`.codex-test/todo-013-flutter-229960-stack.txt`），以及本次任务明确排除的 21:29 dcomp 退出 dump / 20:06 bonsoir dump，它们不是本 TODO 的 GraphicsCapture 制卡崩溃证据；未并入本条修复范围。
- **复发（TODO-031 第五修，2026-06-10）**：含三防线（callback_state 失活 / 成员持有 handler / 析构顺序）的 9:34 Release 构建在 11:53 仍崩（`hibiki.exe.60864.dmp`，栈证据 `.codex-test/todo-031-graphicscapture-fixedbuild-1153-stack.txt`）：栈仍是 `TypedEventHandler::operator()+0x15` `rcx=0`，上游 `coremessaging!DeferredCall::Callback_Dispatch → FirePresentEvent`。判定：已排队的 deferred FirePresentEvent 持帧池强引用（`FirePresentEvent+0x62` 本体跑通），但 teardown 当下的 `Close()`/释放已撤销 WGC 内部 delegate 表/agile-ref，事件 fire 时解析出 null handler 直接 invoke——崩在我们 lambda 之前，三防线全部够不着。第四修（CreateFreeThreadedCaptureFramePool，`47d6ce904`）消灭了崩溃路径，但 **Release 构建下 WebView 纹理不更新（书籍文字全部不显示）**，用户实证 v1 无字 / v2 revert 有字后退回，禁止回潮。
- **[x] ① TODO-031 第五修：保序销毁** — 保持 `CreateCaptureFramePool`（UI 线程 DispatcherQueue 派发，渲染线程模型不变）。`StopInternal()` 不再当场 Close/释放帧池，改为把 `frame_pool_`/`frame_arrived_handler_`/`callback_state` 整组移交 `PendingCaptureTeardown` holder，经 `GraphicsContext::GetDispatcherQueueForCurrentThread()` 拿到的同一个 DispatcherQueue 以 `DispatcherQueuePriority_Low` 排释放 hop。保序依据：① dump 栈证明 FirePresentEvent 走 CoreMessaging DeferredCall，而 `CreateDispatcherQueueController` 即 CoreMessaging.dll 导出，两者同队列；② TryEnqueue 官方文档「The queue will invoke callback serially and in priority order」；③ Low 优先级文档「scheduled when there isn't any other work to process… can be preempted by new incoming High and Normal priority tasks」——只要队列里还有（含晚于 hop 入队的）FirePresentEvent，释放就不会执行。收敛协议：连续 `kCaptureTeardownQuietHops=2` 个「安静」hop（用 `callback_state->frame_events` 计数判定期间无迟到帧 fire）才 `FinalizeCaptureTeardown`（Close+释放）；有迟到帧重新计数；session 已先 Close 故必然终止。`TryEnqueue` 失败仅发生在队列 shutdown（文档：shutdown 后返回 false），此时队列不再派发任何已排队事件，内联释放安全不泄漏。弹窗重开（新 TextureBridge/新 Start()）创建全新 pool/handler/state，与在途 holder 无共享可变状态。根因 `packages/flutter_inappwebview_windows/windows/custom_platform_view/texture_bridge.cc`（StopInternal/ScheduleCaptureTeardown/EnqueueCaptureTeardownHop/FinalizeCaptureTeardown）。
- **[x] ② TODO-031 第五修已加自动化测试** — `hibiki/test/widgets/texture_bridge_stop_guard_test.dart` 红→绿：禁止 `CreateFreeThreadedCaptureFramePool` 回潮（Release 渲染回归）、必须保持 `graphics_context_->CreateCaptureFramePool(`、必须存在 `PendingCaptureTeardown`/`TryEnqueueWithPriority`/`DispatcherQueuePriority_Low`/`kCaptureTeardownQuietHops`、`StopInternal` 本体禁止出现 `pool_closable`（不得当场 Close 帧池）。
