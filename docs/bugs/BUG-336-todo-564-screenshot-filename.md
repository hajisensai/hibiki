## BUG-336 · TODO-564 视频截图文件名太长，改成视频名+播放时刻更语义化
- **报告**：2026-06-19（用户：B02 反馈）· TODO-564
- **真实性**：✅ 真 bug。反馈「视频截图文件名太长，改成视频名+时间之类更有意义」。根因 `hibiki/lib/src/media/video/video_screenshot_filename.dart`（修复前 `videoScreenshotBaseName` 拼成 `hibiki_<视频名>_at_<HHhMMmSSsmmm>_<YYYYMMDD_HHMMSS_mmm>.jpg`，三段时间戳叠加：① `hibiki_` 冗余前缀 ② `_at_` 播放位置带毫秒 ③ 完整墙钟日期+时分秒+毫秒。实测形如 `hibiki_響け！ユーフォニアム 第1話_at_00h01m05s234_20260617_231205_987.jpg`，又长又难读）。前一个 TODO 已让多次截图各不相同（靠墙钟毫秒 + `(n)` 后缀去重），但用力过猛把名字撑长。
- **[x] ① 已修复** — commit `<PENDING>`：`video_screenshot_filename.dart` 把默认名改成语义化 `<视频名>_<HH-MM-SS>.jpg`（如 `響け！ユーフォニアム 第1話_00-01-05.jpg`）。`videoScreenshotBaseName` 去掉 `hibiki_` 前缀、去掉墙钟时间戳整段（删去不再用的 `capturedAt` 参数 + `_captureTimeToken`/`_three`/`_four`）；时间改用**播放位置**（截图记录的本就是「视频的哪一刻」，比截图墙钟时间更有意义），`_playbackTimeToken` 由 `HHhMMmSSsmmm` 改为无毫秒的 `HH-MM-SS`。非法字符清洗 `_safeScreenshotSourceStem`（Windows 非法字符→`_`、保留名前缀、80 rune 截断、Unicode 保留）原样保留。调用点 `video_hibiki_page.dart:5929-5933` 同步去掉 `capturedAt: DateTime.now()`。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/video_screenshot_filename_test.dart` 更新：① 断言新名 `響け！ユーフォニアム 第1話_00-01-05.jpg`（含 Unicode 标题、HH-MM-SS）；② 断言文件名不再含 `hibiki_` 前缀 / `_at_` / 8 位以上连续墙钟数字串；③ **去重契约守卫**：同一视频同一播放秒（90000ms 与 90120ms 都落 00:01:30）得到同名，由 `uniqueVideoScreenshotBaseName` 的 ` (n)` 计数后缀兜唯一（断言 `episode 01_00-01-30 (3).jpg`）；④ `_saveScreenshot` 源码守卫（仍用 `videoScreenshotBaseName(`/`uniqueVideoScreenshotBaseName(`、`fileName: screenshotName`、`subject: screenshotName`、旧固定名负向断言）继续通过。
- **备注**：去重从「靠墙钟毫秒第一层 + (n) 第二层」改为「靠 (n) 第二层兜底」，因新名秒级播放位置同视频同秒会同名——这是合理根因设计，第二层 `uniqueVideoScreenshotBaseName`（临时目录 existsSync）与 `uniqueVideoScreenshotPath`（桌面保存路径）两处都已有 ` (n)` 后缀，连点截图不撞名、不覆盖。扩展名仍 `.jpg`（实际截图就是 JPEG，非用户举例的 .png）。`flutter analyze` 0 问题，`flutter test test/pages/video_screenshot_filename_test.dart` 全绿。真机截图肉眼复测文件名待用户。
