## BUG-485 · 添加本地音频库被复制到C盘，应支持引用原路径
- **报告**：2026-06-30（用户：）
- **真实性**：✅ 真 bug。查词页「添加本地音频数据库」(Yomitan 发音库 SQLite) 链路 `dictionary_settings_dialog_page.dart` onPickLocalDb → `app_model.dart` `importLocalAudioDbFile` → `lib/src/models/local_audio_manager.dart` `importFile` 无条件 `sourceFile.copy(internalPath)`，目标 `_databaseDirectory` = `getApplicationSupportDirectory()` = Windows `AppData\Roaming`（用户说的 C 盘）。TODO-935 给有声书导入加的「引用原文件不复制」开关从未接入这条本地音频库链路 = 遗漏点。根因 `hibiki/lib/src/models/local_audio_manager.dart` 旧 copy 行。
- **[x] ① 已修复** — 仿 935 reference 模式：`LocalAudioManager.importFile` 加 `bool reference = false`，reference=true 跳过 copy 直接返回指向原 `sourcePath` 的 entry（`lib/src/models/local_audio_manager.dart`）；`AppModel.importLocalAudioDbFile` 透传该 bool（`lib/src/models/app_model.dart`）；`AudioSourcesDialog` 加桌面专属 `SwitchListTile`（`isDesktopPlatform` 门控）+ `onPickLocalDb` 改 `Function(bool reference)` 透传（`lib/src/pages/implementations/dictionary_settings_dialog_page.dart`）；wiring 在 `lib/src/settings/settings_schema_lookup.dart` 转发；新增 `_isInternalCopy` 派生「库目录之外=引用条目」，`remove()` 据此跳过删除外部原文件，`pruneOrphans` 只遍历库目录内 `local_audio_*.db` 故天然不碰外部引用。新增 i18n key `local_audio_reference_original` / `_desc`（17 语言）。提交哈希：e72cf4b55c (this commit)
- **[x] ② 已加自动化测试** — 契约测试 `hibiki/test/models/local_audio_manager_test.dart`（reference=true 不 copy 返回原路径 / 源缺失仍抛 / remove 不删外部引用 / pruneOrphans 不碰外部文件）+ 源码守卫 `hibiki/test/tools/local_audio_reference_guard_test.dart`（importFile 有 reference 分支 + 清理跳过外部路径 + 全链路透传）。提交哈希：e72cf4b55c (this commit)
- **备注**：移动端 file_picker 返回缓存临时副本，引用即指向会被系统清掉的文件，故开关仅桌面暴露、移动端恒 false（沿用 TODO-935 的判断）。
