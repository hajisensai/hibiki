## BUG-212 · 书架作者导入后不回显不可编辑+tag竖排参差
- **报告**：2026-06-12（用户：）拆自 TODO-159 子2/子3（TODO-167）
- **真实性**：✅ 真 bug（两处真实缺失）。根因：
  - **子3-a 作者不回显**：`hibiki/lib/src/media/sources/reader_hibiki_source.dart:260` 的 `MediaItem(...)` 构造完全没传 `author`，所以 EPUB 书 `MediaItem.author` 恒 null；而 `epubBooks.author` 列（`packages/hibiki_core/lib/src/database/tables.dart:232`）导入时已写库。详情弹窗 `media_item_dialog_page.dart:126-132` 在 `item.author` 非空时本就会显示，缺的只是 source 没把 DB author 填进 MediaItem。
  - **子3-b 作者不可编辑**：`media_item_edit_dialog_page.dart` 只有标题 override + 封面 override，无 author 输入框；且 `MediaSource` 没有任何 author 写回通道。标题用 preference override（不动主键），因 `bookKey = sanitize(title)` 是主键、`database.dart:1273 updateEpubBookTitle` 故意 throw（re-key 未做）；但 `author` 不是主键，可直接 UPDATE `epubBooks.author`。
  - **子2 tag 竖排参差**：`reader_hibiki_history_page.dart:470 _adaptiveTagColumn` 用 `Column(crossAxisAlignment.start)` 竖排放进卡片左上角 `Positioned` 覆盖层，每个 `HibikiTagChip` 宽=文字宽（内部 `Row(mainAxisSize.min)`）→ 同名几个 tag 一行长一行短。
- **[x] ① 已修复** — 子3-a 回填 `reader_hibiki_source.dart` `_bookToMediaItem` 的 `MediaItem(author: book.author)`；子3-b 新增 DB 写回 `database.dart updateEpubBookAuthor`（plain UPDATE，非主键无 re-key）+ `MediaSource.supportsAuthorEdit`/`setAuthorFromMediaItem`（base no-op，ReaderHibikiSource override 直写 epubBooks.author）+ `media_item_edit_dialog_page.dart` 加 author TextField（gated on supportsAuthorEdit）并 executeSave 写回；子3-c i18n `book_edit_author`（17 文件，i18n_sync.dart）。子2 `reader_hibiki_history_page.dart _adaptiveTagColumn` 改用 `_uniformWidthTagColumn`（IntrinsicWidth + CrossAxisAlignment.stretch），竖排标签统一为最宽 chip 宽度消除参差。提交：见报告 commit。
- **[x] ② 已加自动化测试** — `test/media/sources/reader_hibiki_source_test.dart`：setAuthorFromMediaItem 写穿 epubBooks.author（真 DB）/ trim+空作者清 NULL / updateEpubBookAuthor 保持 bookKey 不 re-key / 4 条源码守卫（author 回填+写回+详情显示+编辑字段）。`test/pages/media_item_dialog_author_test.dart`：MediaItemDialogFrame 有 author 显示行 / null 时隐藏（子3-a 回显 widget 行为）。`test/pages/adaptive_tag_column_layout_test.dart`：IntrinsicWidth+stretch 让不同宽 chip 同宽（子2 行为）+ 源码守卫（_adaptiveTagColumn 不再用裸 start 列）。全套 `flutter test` 4256 passed / 2 skipped / 0 failed。
- **备注**：作者写回机制选「直写 epubBooks.author 列」（与导入一致、单一真相源，author 非主键不触发 re-key）。SRT 书有 author 列但走单独 _buildSrtCard 渲染、不经 MediaItem 编辑链路；视频书无 author 列。本次 supportsAuthorEdit 仅 ReaderHibikiSource 为 true，SRT/视频书不显示作者字段、零误伤。号可能与并发 worktree 撞，保留号由 integration owner 合并时改。
