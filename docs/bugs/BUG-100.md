## BUG-100 · 阿拉伯语（及一切空格分词语言）查词把单词从中间砍出无关词头：搜 "أمنيات العيد" 出来 "أم"(母亲)
- **报告**：2026-06-07（用户：「阿拉伯语查词有问题，أمنيات العيد，出来的是 …」+ 截图：查 "أمنيات العيد"(开斋节的祝愿) 结果只出词头 "أم"=母親）。采番注：本条初记 096，rebase 到 develop 时 096–099 已被并发提交占用（096=书内设置 master-detail、097=内链白屏、098=弹窗遮挡、099=翻页方向键），改取 100。
- **真实性**：✅ **真 bug（沿真实代码路径定位）**。首页「查词」框 → `home_dictionary_page.dart:_search` → `app_model.dart:searchDictionary` → `HoshiDicts.lookup`（**带去屈折的扫描版**，非精确 `query`）→ native `Lookup::lookup`（`native/hoshidicts/hoshidicts_src/lookup.cpp:45-86`）。该扫描器从串首锚定、**每次砍 1 个码点**（`lookup.cpp:50` 循环 + `:84` `utf8::prior`）一路缩到 1，每个长度做精确哈希查询。这对日语（无空格、短词合法）正确，但对阿拉伯/拉丁/西里尔等**空格分词**语言会**在单词中间乱砍**：`"أمنيات"`(6 字母) 被砍到 2 字母 `"أم"` 恰好是词典词头(=母亲) → 命中 → 排序按「匹配最长」选中 → 返回完全无关的 "أم/母親"。等价于搜英文 `mother` 而词典里有 `mo` 就返回 `mo`。`text_processor` 对阿拉伯语只去 harakat/tatweel，与此 bug 无关。
- **对齐做法（用户「yomitan 怎么做的」）**：Yomitan 用 `translation.searchResolution` 控制 translator 裁剪循环（`translator.js:_getNextSubstring`）：`'letter'`=每次砍 1 字符（=本仓现状，日语对）；`'word'`=正则 `/[^\p{Letter}][\p{Letter}\p{Number}]*$/u` **一次砍掉整个末尾单词**，永不产出词中片段。空格分词语言(ar/en/ru)推荐配 `'word'`，CJK/韩语硬排除恒走字符级。
- **[x] ① 已修复** — 本提交（自含修复+测试+本条目）。**根因修（消除特例，不按语言枚举开关）**：新增独立纯函数模块 `native/hoshidicts/hoshidicts_src/scan/word_scan.{hpp,cpp}` 的 `scan_candidates(text, scan_length)`，复刻原「从首锚定、由长到短」枚举，但**禁止把切点落在两个「空格分词类字母」之间**（拉丁/西里尔/希腊/阿拉伯/希伯来/亚美尼亚/格鲁吉亚字母；CJK 汉字/假名/谚文不属此类 → 它们之间所有切点照旧合法 → **日语逐码点零变化**），并丢弃以空白结尾的冗余前缀。`Lookup::lookup` 的内联缩短循环改为 `for (const auto& search_str : scan_candidates(lookup_string, scan_length))`，去重/排序/查询逻辑一字未动。效果上对纯阿拉伯/拉丁 ≡ Yomitan `'word'`、对 CJK ≡ `'letter'`，且零配置、不依赖「当前语言」（比 Yomitan 全局设置更稳，规避其 #1861「切语言不自动套用」）。同时修好首页查词框 **与** 阅读器/视频/有声书点词（都走同一 `Lookup::lookup`）。对 `"أمنيات العيد"` 现只查整串与 `"أمنيات"` 两候选，均未收录 → 返回空（诚实），不再误导出「母親」。
- **[x] ② 已加自动化测试** — `native/hoshidicts/tests/word_scan_test.cpp`（纯内存单测，仿 `text_processor_test.cpp`，VS2022 `cl` 跑，`tests/run_word_scan_test.bat`，PASS）：`scan_candidates("أمنيات العيد")=={整串,"أمنيات"}` 且**绝不含 "أم"**（核心回归）、阿拉伯单词只它自己、拉丁 `"running fast"={整串,"running"}`、日语 `"母親"={"母親","母"}`（逐码点不变）、混合 `"猫dog"={"猫dog","猫"}`、scan_length 截窗、空串/全空白={}。RED（stub 返空，6 FAIL）→ GREEN 全验。
- **备注**：词典查词/native 类。MY 改动 `word_scan.cpp` 编译 + 单测 PASS、`lookup.cpp` 对真实头文件 `/Zs` 语法编译通过、CMake 已登记 `word_scan.cpp`。**全平台 FFI DLL 重建 + 真机查词复测待用户**（项目惯例：native 改动重建+真机为用户环节；本机未跑完整 CMake 全量 DLL 链接）：真机重建后，阿拉伯/英文查词不再返回词中碎片；日语查词行为不变。**已知局限（非本 bug）**：词典未收录的阿拉伯词仍查不到（返回空）——若要进一步召回需补 `ال` 定冠词剥离/阿拉伯形态还原，属独立增强。
