## BUG-103 · 视频统计纵坐标恒显示「0m 0m 0m 0m 0m」（观看时长不足 1 分钟时被整除成 0）
- **报告**：2026-06-07（用户：「修复视频统计里面纵坐标是 0m 0m 0m 0m 0m 的问题」，同批需求含删字数统计 + 弹窗收藏按钮 + 收藏/制卡计入统计）。采番注：初记 092，rebase 到 develop 时 092–102 已被并发提交占用，改取 103。
- **真实性**：✅ **真 bug（沿真实代码路径定位）**。今日按小时图 `StatHourlyChartPainter`（`stat_charts.dart`）纵轴标签经 `_formatMs(ms) = '${ms ~/ 60000}m'`。当当日观看时长峰值不足 1 分钟（如刚看几十秒）时，4 等分刻度值 `0 / max/4 / … / max` 全部 `< 60000` → 整除成 0 → 整条纵轴退化成「0m 0m 0m 0m 0m」，看不出量级。删字数后日图 `StatBarChartPainter` 也改画观看时长，会撞同一格式化退化。
- **[x] ① 已修复** — 本提交。**根因修（格式化器按量级回退到秒，而非一律按分钟整除）**：新增共享纯函数 `formatStatDurationAxis(ms)`（`stat_charts.dart`）：`>=1h` 显 `Xh`、`>=1m` 显 `Xm`、`>0` 回退 `Xs`、`0` 显 `0`。小时图纵轴改用之；`StatBarChartPainter` 参数化 `valueOf`/`labelFormatter`（顶层静态 tear-off 保 `shouldRepaint` 稳定），视频日图传 `statMsValue` + `formatStatDurationAxis`（阅读日图仍默认画字数，不变）。同提交：①视频统计删字幕字数展示（summary 以观看时长为主、byVideo 按 ms 排行、日图画 ms）②查词弹窗（书内阅读+视频共用 DictionaryPageMixin）加「☆/★」收藏按钮 ③新建 `favorite_words`/`mining_statistics` 表（schema v22→v23），收藏/制卡按来源 book/video 计入各自统计页四张汇总卡片。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/stat_activity_test.dart`（formatStatDurationAxis 30s/45s 回退到秒、1.5 分钟量级 4 刻度不再全 `0m`、分桶）+ `favorite_button_wiring_static_test.dart`（收藏按钮端到端守卫 6 项）+ `migration_test.dart` v22→v23 CRUD/来源隔离 + `video_stat_aggregates_test.dart` 按 ms 排行。全量 flutter test 0 失败。
- **备注**：视频统计图表 / 查词弹窗收藏 / 收藏制卡统计类。代码正确 + 单测无回归；**真机肉眼复测待用户**：看一段不足 1 分钟的视频后进视频统计，今日小时图与 30 天图纵轴应显示秒/分而非全 0m；查词弹窗有收藏按钮可 toggle；统计页「制卡 N · 收藏 N」随操作增长。
