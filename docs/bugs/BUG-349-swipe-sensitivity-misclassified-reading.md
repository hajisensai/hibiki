## BUG-349 · TODO-625 滑动关闭灵敏度错置阅读分类应归查词
- **报告**：2026-06-20（用户：）
- **真实性**：✅ 真 bug（设置项归属分类错位）。`enable_swipe_to_close`（允许水平滑动关闭查词弹窗）早被 TODO-436 移到查词分类（`ReaderGroup.lookup`），但配套控制**同一查词弹窗手势**的 `dismiss_swipe_sensitivity`（滑动关闭灵敏度阈值）滞留在阅读分类（`ReaderGroup.behavior`），导致开关在「查词」段、灵敏度却在「阅读」段，两个配套项被拆到两个分类。根因 `hibiki/lib/src/settings/settings_schema.dart:775`（旧位置，`reading_controls.dismiss_swipe_sensitivity` 的 `ReaderPlacement(group: ReaderGroup.behavior, order: 5)`）。排查 reading/lookup 两区，唯一错位项就这一个（翻页灵敏度/滚轮间隔真控阅读翻页；弹窗显示/行为项已正确在查词）。
- **[x] ① 已修复** — 把 `SettingsSliderItem(id: 'reading_controls.dismiss_swipe_sensitivity')` 从 reading destination「导航/控制」section 整块剪切，粘贴进 `_lookupDestination()` 的「查词行为」section（`t.settings_section_lookup_behavior`），紧邻 `enable_swipe_to_close` 开关；`ReaderPlacement` 从 `(group: ReaderGroup.behavior, order: 5)` 改为 `(group: ReaderGroup.lookup, order: 4)`（与开关 order:3 相邻）。**只动展示归属，不动行为**：id/i18n key/偏好 key（`'dismiss_swipe_sensitivity'`）/value/onChanged/min/max/divisions/label 全原样保留，消费点（`dictionary_popup_layer.dart` / `popup_dictionary_page.dart`）未动；id 保持 `'reading_controls.dismiss_swipe_sensitivity'` 前缀作向后兼容（持久化无关展示分类，比照 `enable_swipe_to_close` 同前缀放 lookup 区先例）。文件 `hibiki/lib/src/settings/settings_schema.dart:1129`（新位置）（提交于分支 todo-625-swipe-sensitivity-lookup）。
- **[x] ② 已加自动化测试** — ①守卫 `hibiki/test/settings/settings_schema_coverage_test.dart`：覆盖键 `reading/Swipe dismiss sensitivity` → `lookup/Swipe dismiss sensitivity`（coverage 键为 `destId/title` 复合，destId 随分类从 reading 变 lookup，title 不变），保证 `stillUnaccounted=0`。②守卫 `hibiki/test/settings/settings_redesign_static_test.dart`：在 `enable_swipe_to_close` 的 `ReaderGroup.lookup` 锁定断言后追加 `dismiss_swipe_sensitivity` 的并列锁定断言（`contains('group: ReaderGroup.lookup')` + `isNot(contains('group: ReaderGroup.behavior'))`），防回归滞留阅读分类。`flutter test test/settings/`：148 通过（1 失败为预存 `md3` 的 `video_side_panel.dart: VisualDensity.compact`，TODO-611/613 引入，与本改动无关）。
- **备注**：纯展示分类归位，不触碰持久化键与运行时行为，读穿 DB/偏好不变。真机待验：查词分类应见「滑动关闭灵敏度」滑块紧邻「允许水平滑动关闭」开关；阅读分类不再出现该滑块；行为/偏好读写不变。
