## BUG-408 · 视频空格无反应(c152fcd91全局吞裸空格+视频失焦)
- **报告**：2026-06-23（用户：）
- **真实性**：✅ 真 bug。根因不是空格映射丢失（映射完好：`hibiki/lib/src/shortcuts/shortcut_defaults.dart:190` 默认绑 videoTogglePlayPause 含裸 Space → `hibiki/lib/src/media/video/video_player_shortcuts.dart:124` 装进 media_kit 桌面主题 `controls_theme.part.dart:62` 的 keyboardShortcuts），而是焦点归属：该映射**只在 `_videoFocusNode`（debugLabel videoKeyboard）或 controls 内置 Focus 精确持焦时生效**。回归源 `c152fcd91`（2026-06-08）首次在全局 Shortcuts 加无条件裸空格 `DoNothingIntent`（`hibiki/lib/src/shortcuts/global_navigation.dart:232`，不在 focusNavigationEnabled 守卫内，`DoNothingAction.consumesKey == true` 真消费按键）。任何「焦点落在视频页子树但不精确在 `_videoFocusNode`」的瞬间（关对话框/菜单后短暂失焦、点了非视频区控件等），裸空格上浮到全局 `DoNothingIntent` 被主动 handled 吞掉 →「按了没反应」。此前失焦时裸空格落 WidgetsApp 默认无害，故掩盖；c152fcd91 后所有「焦点没精确落视频上」路径集体显形。
- **[x] ① 已修复** — `hibiki/lib/src/pages/implementations/video_hibiki_page.dart` `_buildScaffold` 在 `_pageDropTarget` 外层加**页内局部** `CallbackShortcuts`（新增 `_withPageSpaceOverride` helper）绑裸 `LogicalKeyboardKey.space` → `_runWhenImmersiveAllowsFullControls(() => unawaited(controller.playOrPause()))`，位于全局 `DoNothingIntent` 之下、离视频更近。焦点落在视频页子树内任意节点时空格都先被这层消费，永不下沉到全局中和层；与注册表 togglePlayPause 同语义（经沉浸锁门控），不触全局中和（非视频界面空格仍中和，不破坏 TODO-112「空格不确认焦点」）。提交：<待填 SHA>
- **[x] ② 已加自动化测试** — `hibiki/test/pages/video_space_playpause_override_test.dart`：①widget 行为正向（焦点落普通节点、不调任何视频节点 requestFocus 时裸空格仍触发 playOrPause）②负向对照（撤掉页内局部覆盖 → 裸空格被全局 DoNothingIntent 吞，复现回归）③源码守卫（_withPageSpaceOverride 用 CallbackShortcuts + 裸空格 + 沉浸锁门控 + playOrPause，且 _buildScaffold 真套用）。提交：<待填 SHA>
- **备注**：全局裸空格中和不变式不破坏——`test/shortcuts/global_space_no_activate_test.dart`（裸空格不激活焦点控件）仍绿。media_kit 的 keyboardShortcuts 在精确持焦时是更近作用域、先消费，与本层不冲突。
