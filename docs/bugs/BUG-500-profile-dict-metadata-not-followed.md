## BUG-500 · TODO-1077 切换Profile词典设置不跟随
- **报告**：2026-07-01（用户：）
- **真实性**：✅ 真 bug。词典的启用/排序/来源/查词语言可见性真相源是独立 Drift 表 `dictionary_metadata`（`packages/hibiki_core/lib/src/database/tables.dart:201-216`，`name` 主键=启用列表，`order` 排序，`formatKey` 来源，`type`，`hiddenLanguagesJson`/`collapsedLanguagesJson` 语言可见性），运行期缓存在 `hibiki/lib/src/models/dictionary_repository.dart:51-153`。而 Profile 快照读写只覆盖 `preferences` 表 + Anki SharedPreferences 两个命名空间：`snapshotCurrentSettings`（`hibiki/lib/src/profile/profile_repository.dart:109-143`）与 `applyProfile`（同文件 `:145-215`）全程零触碰 `dictionary_metadata`，故该表全局单实例、所有 Profile 共享，换 Profile 时词典配置纹丝不动。
- **[x] ① 已修复** — 提交 35b4ee13f。新增独立 category `ProfileKeys.categoryDictionaryMeta = 'dictionary_meta'`（`hibiki/lib/src/profile/profile_keys.dart`，不复用 legacy `'dictionary'`）。`snapshotCurrentSettings` 追加 `getAllDictionaryMetadata()`，逐行序列化成一条 profile_settings（key=词典 name，value=JSON:order/formatKey/type/metadataJson/hiddenLanguagesJson/collapsedLanguagesJson）。`applyProfile` switch 收集该 category，在既有事务内对 `dictionary_metadata` 做「以快照为准替换」（upsert 快照行 + 删除快照中不存在的行，复用 `upsertDictionaryMeta`/`deleteDictionaryMeta`）。护栏：`hasDictMeta` sentinel —— 旧快照无该 category 行时跳过替换，绝不把「无快照」误当「空词典」删光全局词典；单行 value 坏（`_decodeDictionaryMeta` 返 null）时跳过降级不 abort 整个 apply。切 Profile 后在 `ProfileViewModel` 的 `onApplied`（`hibiki/lib/src/profile/profile_view_model.dart`）调用新增的 `AppModel.reloadDictionariesFromDb()`（`hibiki/lib/src/models/app_model.dart`）：`dictRepo.loadFromDb()` + `_rebuildDictPathsCacheAsync()`（native FFI 引擎重载）+ `clearDictionaryResultsCache()` + `dictionarySearchAgainNotifier` 通知已开查词页重查。exportProfile/importProfile 按 category/key/value 通用透传，新 category 自动随行；导入目标机缺词典库时 apply 的 replace 语义只写快照行，不产生对不存在词典的悬空引用（词典资源目录缺失时 `_rebuildDictPathsCache` 的 `exists` 门控跳过）。
- **[x] ② 已加自动化测试** — 提交 35b4ee13f，文件 `hibiki/test/profile/profile_repository_test.dart` 新增 group `dictionary_metadata follows profile (TODO-1077)`：① 快照→切换→applyProfile 回环后启用列表/order/hidden 都跟随（Other 被剪、JMdict 复原、Daijirin 的 hidden=['en'] 还原）；② order 随 Profile 切换翻转；③ 护栏：旧快照无 dictionary_meta category 时绝不删光全局词典；④ 坏 JSON value 行跳过、apply 仍成功。`flutter test test/profile/profile_repository_test.dart` 全 17 测试通过（含新增 4 条）。
- **备注**：BUG 号由 `tool/bug.dart new` 自动取，集成 owner 落地时若与并发 agent 撞号会重编号。
