## BUG-228 · 字幕列表仿asbplayer精致度 + 引入画面比例设置 (TODO-152)
- **报告**：2026-06-12（用户：）
- **真实性**：✅ enhancement（非 bug）。子A=字幕跳转列表（`hibiki/lib/src/media/video/video_subtitle_jump_panel.dart`）已是 asbplayer 式竖列但每行只有 InkWell+时间戳+文本，缺行内操作按钮/hover/工具栏；子B=窗口模式 Video 在 `hibiki/lib/src/pages/implementations/video_hibiki_page.dart:4118` 硬编码 `fit:BoxFit.cover`、全屏走 `params.fit`（默认 contain），两处不可由用户选画面比例。
- **采番**：bug.dart new 在本 worktree 取 227，但 227 号已被并发任务（TODO-171 改 `video_subtitle_style`）占用，手动改号 **228** 避免撞号；本地 `docs/bugs/` 在 226 后直接是 228（227 为并发空号），`reindex` 已据真实文件重建索引、移除前任残留的错误 BUG-227 索引行。
- **[x] ① 已实现** — 子A：`video_subtitle_jump_panel.dart:298-389` 给当前句/hover 行加行内按钮（跳转 `play_arrow` / 复制 `content_copy_outlined` / 收藏 `star`）+ 整行 hover 浅高亮 + 头部工具栏（字号 A-/A+ 步进 + 自动滚动开关，`:220-280`）；**不做双语行**（`AudioCue` 只有单条 `text`，无第二字幕轨数据源，诚实地不提供该开关）。新增回调 `onCopyCue`/`onFavoriteCue`/`isCueFavorited`，面板仍只读 controller、不直接调播放器；复制/收藏回调落在 `video_hibiki_page.dart:1681-1751`（`_copyCueText` 复用既有 `t.copied_to_clipboard` toast、`_toggleFavoriteCueForVideo` 走 `FavoriteSentenceRepository` 视频句键、`_favoritedVideoSentences` 缓存驱动同步星标）。子B：新增 `VideoFitMode` 枚举偏好（`preferences_repository.dart:35-65` + `videoFitModeToBoxFit` 纯函数换算，存字符串名，参照 `DesktopClipboardWindowMode`），窗口模式（`video_hibiki_page.dart:4227`）+ 全屏路由（`:2404`）的 `fit` 统一改 `fit: videoFitModeToBoxFit(_videoFitMode)` 跟随同一偏好（默认 `cover`=保持比例占满无黑边，与旧硬编码 `BoxFit.cover` 行为一致 → 向后兼容 TODO-122），设置入口 `video_quick_settings_sheet.dart:400-449` 播放分类（`AdaptiveSettingsPickerRow`：占满/适应加黑边/拉伸）。提交哈希：`57bb9dc0b`（worktree-agent-ad19b39b043961f21，未合 develop / 未 push，待真机）
- **[x] ② 已加自动化测试** — 子A：`test/media/video/video_subtitle_jump_panel_test.dart`（行内跳转/复制/收藏按钮存在 + 复制/收藏回调触发 + 实心/空心星 + 头部字号步进/自动滚动开关 + 源码守卫 i18n key）；子B：`test/pages/video_fit_mode_test.dart`（`VideoFitMode↔BoxFit` 三档换算 + `fromStorage` 往返 + 未知值降级 cover + 偏好 in-memory DB 往返 + video 页 fit 跟随偏好源码守卫）+ `test/pages/video_quick_settings_sheet_test.dart`（画面缩放 picker 行存在 + 选档落 `onVideoFitModeChanged` 回调）+ `test/pages/video_window_aspect_lock_static_test.dart`（旧 TODO-122 硬编码 cover 守卫升级为「窗口+全屏经 `videoFitModeToBoxFit` 跟随偏好、默认 cover 不破坏」）。`flutter analyze` 0 + `flutter test` 全套 4329 绿。
- **备注**：用户诉求 ①抄 asbplayer 字幕列表按钮+效果 ②画面缩窄加黑边/引入画面比例设置（澄清：默认保持比例像 mpv 无黑边）。真机看缩放/字幕列表交互待用户。
