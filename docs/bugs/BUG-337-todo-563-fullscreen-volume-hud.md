## BUG-337 · TODO-563 滑动手势音量/亮度 HUD 桌面与全屏也应显示（不止手机窗口）
- **报告**：2026-06-19（用户：B02 反馈）· TODO-563
- **真实性**：✅ 真 bug（部分）。反馈「滑动手势 HUD 电脑也应该有·不止手机」。沿真实代码路径核查后分两层：
  - **窗口模式音量 HUD（桌面已有，非缺口）**：HUD overlay `_buildLevelHudOverlay()` 在窗口侧主 Stack `hibiki/lib/src/pages/implementations/video_hibiki_page.dart:7693` **无平台门控**挂载；所有改音量入口（滑条 / 桌面滚轮 `_onVolumeWheel`→`_adjustVolume`→`_applyUserVideoVolume` `:4188/:5276/:4200`、键盘音量键 `:3337` 绑定、静音、移动竖滑）统一走 `_applyUserVideoVolume`，其 `:4209` `if (mounted) _showVolumeOsd(clamped);` 同样无平台门控 → 桌面窗口调音量（滚轮/快捷键）本就会弹右侧音量百分比 HUD。这条在当前代码已成立。
  - **全屏模式音量/亮度 HUD（真缺口，本次修复）**：全屏走独立 `PageRoute`（`_pushNeutralizedVideoFullscreen` `:3452`，pageBuilder `:3482-3558`），该路由的 widget 树只包了 `Video` + 字幕面板，**没有挂 `_buildLevelHudOverlay()` / `_buildOsdOverlay()`** → 桌面/移动全屏时调音量（滚轮 / 键盘音量键 / 竖滑）都看不到 HUD 反馈。这是 HUD「全屏没有」的真根因。
  - **亮度桌面诚实不可用（保留现状）**：`ScreenBrightnessController.canControl => isMobilePlatform`（`hibiki/lib/src/platform/screen_brightness_controller.dart:29`），桌面无统一窗口级背光 API，亮度手势/回调/快照三处都被 `canControl` 门控；桌面只调音量、不调亮度，HUD 亮度侧仅移动出现，属平台限制不强行造假。
- **[x] ① 已修复** — commit `<PENDING>`：在全屏路由 pageBuilder 把内容统一经新 helper `_fullscreenContentWithOverlays(Widget content)`（`video_hibiki_page.dart`，紧接 `_videoWithSubtitlePanel` 之后）包成 `Stack(fit: expand, [content, _buildOsdOverlay(), _buildLevelHudOverlay()])`，让全屏也叠同一对页面级 overlay。两层都读窗口侧同一真相源 notifier（`_levelHudNotifier` / `_osdNotifier`）且为 `IgnorePointer` 全屏覆盖层，不抢字幕/控制条手势。改动点：`:3539-3557`（builder 返回经 helper 包裹）+ 新 helper。窗口侧无平台门控故桌面/移动窗口音量 HUD 维持原状；全屏现在桌面/移动均显示音量 HUD（亮度桌面仍 no-op、仅移动出现）。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/video_volume_osd_guard_test.dart` 新增 `fullscreen route mounts the same page-level volume/brightness HUD`：断言 ① 存在 helper `_fullscreenContentWithOverlays(Widget content)`，② helper 内同时叠 `_buildLevelHudOverlay()` 与 `_buildOsdOverlay()`，③ 全屏路由 `_pushNeutralizedVideoFullscreen` 区域经 `_fullscreenContentWithOverlays(` 包裹内容（HUD 不被全屏静默丢弃）。撤掉全屏 helper 接线即转红。该文件既有 12 例（含窗口侧 HUD 平台无关行为）全绿。
- **备注**：窗口侧桌面音量 HUD 原已存在，本次主修「全屏没有 HUD」这条真缺口，顺带桌面全屏滚轮/快捷键调音量首次有反馈。亮度桌面不可控是诚实平台降级，未强造。`flutter analyze` 0 问题。**真机/真窗口复测待用户**：桌面（Windows）窗口 + 全屏滚轮/音量键调音量看右侧 HUD；移动全屏竖滑看左亮度/右音量 HUD。
