## BUG-332 · 视频上一句/下一句跳转跳过头（TODO-571）
- **报告**：2026-06-19（用户：B03 验收第 9 条「感觉跳过头了？」）
- **真实性**：✅ 真 bug（**真过头，非听感**）。根因 `hibiki/lib/src/media/video/video_player_controller.dart:1788`（`nextCueIndexFor`）/ `:1811`（`prevCueIndexFor`）/ `:1851`（`prevSeekDecisionFor`）按偏前物理 position 反推当前句，与 `skipToCue`（`:1596`）的 preRoll 偏前 seek 落点（`kCueSeekPreRollMs=180`，`:1589`；落点算法 `cueSeekTargetMs` `:1668`）语义错配。
  - 复现链（cue 间有小 gap，真实 SRT/ASS 常见）：用户在 cueN 句中按「上一句」→ 目标 cue(N-1)，但 `skipToCue` 为吸收 media_kit 关键帧吸附（BUG-259 听感）故意把 seek 落点偏到 `cue(N-1).startMs - 180`。该偏前落点**退进了 cue(N-2) 的时间窗**。再按「上一句」时，`prevCueIndexFor` 用这个偏前 position 经 `findCueIndex` 命中 cue(N-2) → `hit-1` = cue(N-3) → **跳过头（跳到 N-2 而非相邻 N-1）**。
  - 对称副作用：连续「下一句」时偏前落点 `< 目标句 startMs`，`_floorCueIndexByPosition` 据 `startMs<=pos` 把当前句反推成目标句的上一句 → `+1` 又指回刚跳到的那句 → **原地不动**。
  - 与 TODO-565 关系：565 的 `_seekTargetCueIndex` 快照只参与高亮 snap（`_syncCueForPosition`/`_applySeekTargetSnap`），**没有参与跳句目标决策**，故跳句仍按偏前 position 反推出错。本修复正是复用该快照作为跳句锚，不破坏 565 的 snap 高亮与宽限。
- **[x] ① 已修复** — `nextCueIndexFor`/`prevCueIndexFor`/`prevSeekDecisionFor` 新增可选 `anchorIndex` 参数；4 个调用方（`skipToNextCue` `:1693` / `skipToPrevCue` `:1711` / `skipToNextCueOrSeekForward` `:1757` / `skipToPrevCueOrSeekBack` `:1725`）传入 `_seekTargetCueIndex`（565 已维护其在 preRoll 引导窗口内的生命周期）。锚存活且在范围内时严格 `anchor±1`，绕过按偏前 position 反推；锚为 null（首次从自然播放跳）或越界（脏快照）时退回原 position 逻辑，零行为变化。**保留 preRoll 听感落点不变**，只修「从偏前落点反推当前句」这层语义错配。`hibiki/lib/src/media/video/video_player_controller.dart`（提交见末尾）。
- **[x] ② 已加自动化测试** — `hibiki/test/media/video/video_player_controller_test.dart` 新增 group「TODO-571 锚定上次主动跳转目标」8 个用例：anchor 存活时下一句严格 N+1（不原地）、上一句严格 N-1（不跳过头 N-2）、连续跳句 anchor 链逐句推进、首/末句边界 no-op、越界脏快照退回 position、`anchor==null` 向后兼容、`prevSeekDecisionFor` 锚定取相邻句。撤掉源码 anchor 分支后精确转红（`Expected:<2> Actual:<1>` 下一句原地、`Expected:<1> Actual:<0>` 上一句跳过头），向后兼容用例仍绿。`flutter test test/media/video` 全绿（+835，2 个 ffmpeg skip）。
- **备注**：真机待验（host 无 libmpv，端到端 seek 落点无法单测，已用纯函数推演脚本 + 单测钉死决策层）。需在真实视频上连续按上一句/下一句确认逐句推进、不跳过头、不原地。
