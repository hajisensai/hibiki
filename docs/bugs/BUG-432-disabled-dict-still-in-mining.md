## BUG-432 · 禁用词典制卡时仍附带该词典释义
- **报告**：2026-06-26（用户：TODO-865）
- **真实性**：✅ 真 bug（BUG-419 的姊妹漏洞，同根因第二条泄漏路径）——词典管理里关闭（show/hide 开关 off）某本 term 词典后，查词弹窗已不再显示它的释义（BUG-419 已修），但**制卡（Anki mining）时该词典的释义仍被写进卡片 glossary 字段**。根因：hidden term 词典在 FFI 引擎里仍被注册、仍返回 glossaries（`hibiki/lib/src/models/app_model.dart` `bucketDictPaths` 的 `case DictionaryType.term:` 故意不按 hidden 过滤，按设计在渲染期过滤），因此 `entry.glossaries` 仍含被禁用词典义项。BUG-419 只修了可视查词弹窗（`createGlossarySectionWrapper`，`hibiki/assets/popup/popup.js:1692`）；制卡走另一条独立的字段组装路径 `buildMinePayload` → `constructGlossaryHtml`（`popup.js:700`）/ `constructSingleGlossaryHtml`（`popup.js:644`），这条路径从未消费 `window.hiddenDictionaryNames`，把 hidden 词典义项原样塞进 `glossary` / `singleGlossaries` / `glossaryFirst` 字段。宿主 `packages/hibiki_anki/lib/src/base_anki_repository.dart:289` `buildMinedFields` 只做 handlebars 渲染 + 媒体占位符替换，信任 JS 端给的 glossary 已正确，故泄漏在 JS 端。frequency/pitch 字段在 bucket 级（`app_model.dart` :216/:218）已按 hidden 过滤，不泄漏。影响所有走 WebView 弹窗制卡的表面（reader / video / 有声书 / 首页查词 / 悬浮窗，均共用 `buildMinePayload`）。
- **[x] ① 已修复** — 在制卡字段组装的两个函数各加一行 hidden 过滤，与 BUG-419 完全同款（同源谓词 `window.hiddenDictionaryNames`、零新概念）：`constructSingleGlossaryHtml`（`popup.js:652`，forEach 体首行 `if (hiddenDictionaryNames.includes(g.dictionary)) return;`，`const hiddenDictionaryNames = window.hiddenDictionaryNames || [];` 提到 forEach 之前）+ `constructGlossaryHtml`（`popup.js:710`，同款）。不动 frequency/pitch（已在 bucket 级过滤），不在 Dart 宿主层反解析 HTML。提交：56632bb2e。
- **[x] ② 已加自动化测试** — `hibiki/test/pages/popup_mining_hidden_dictionary_test.dart`（Node 真执行 popup.js `constructGlossaryHtml` / `constructSingleGlossaryHtml`，断言禁用词典不出现在 multi glossary 的 `data-dictionary` / single glossaries 的 key；仅 hidden 词典的词条产出空 `<ol></ol>` / `{}`；无 node 时 skip。另有源码守卫：静态扫描 popup.js 两函数体内各含 `hiddenDictionaryNames.includes(` 过滤行）；行为 harness `hibiki/test/pages/popup_mining_hidden_dictionary_test.js`。撤掉两行过滤实测转红、加回转绿。
- **备注**：未真机复测（无设备）；逻辑层 + Node 行为层已闭环。真机门禁：用户在词典管理关一本 term 词典 → 查词制卡 → Anki 卡片 glossary 字段不含该词典释义。
