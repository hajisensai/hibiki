## BUG-305 · 播放列表不会自动连播且下一集字幕列表初始空
- **报告**：2026-06-16（用户：TODO-447：剧集列表不会自动连播；除第一集外字幕/“章节”列表先空或延迟）
- **真实性**：✅ 真 bug。根因分两层：① `VideoPlayerController` 只订阅 `player.stream.playing`，没有订阅 `player.stream.completed`，页面也没有 EOF 回调入口，所以播到末尾不会触发播放列表 `_switchEpisode`；② 切到下一集前只预热当前视频字幕，后台抽取内封文本字幕时字幕列表没有 loading 状态，用户会先看到空列表/emptyHint，等抽取完成后才出现字幕。
- **[x] ① 已修复** — `hibiki/lib/src/media/video/video_player_controller.dart` 增加 `_completedSub`、每次 `load` 的 completed 去重位、`setOnCompleted` seam/debug hook、load/dispose 取消订阅 guard，并暴露 `isSubtitleCuesLoading`；`hibiki/lib/src/pages/implementations/video_hibiki_page.dart` 在 `_applyLoad` 调 `controller.load` 前绑定 completed 回调，加入 `_autoAdvanceInFlight`、mounted/playlist/最后一集保护，非最后一集沿用 `_switchEpisode(_currentEpisode + 1)`，并新增下一集字幕预热去重；`hibiki/lib/src/media/video/video_subtitle_jump_panel.dart` 区分字幕加载中与真正空列表，loading 文案走 `hibiki/tool/i18n_sync.dart` + `slang`。提交：本次 TODO-447 施工提交。
- **[x] ② 已加自动化测试** — `hibiki/test/media/video/video_player_controller_test.dart` 覆盖 completed=false 不触发、true 每次 load 只触发一次、reload 后重新触发、替换 stream/load/dispose 取消订阅；`hibiki/test/media/video/m3u8_playlist_test.dart` 覆盖最后一集不越界、非最后一集下一集索引和预热路径去重；`hibiki/test/pages/video_playlist_auto_advance_guard_static_test.dart` 守卫页面先绑定回调、in-flight/mounted/playlist/最后一集保护与 `_switchEpisode(nextEpisode)`；`hibiki/test/media/video/video_subtitle_source_test.dart` 覆盖两集 fake ffmpeg 缓存复用；`hibiki/test/media/video/video_subtitle_jump_panel_test.dart` 覆盖字幕 loading 状态。
- **备注**：自动化覆盖了真实代码分支和边界守卫；真实短视频 EOF 验证受当前 Windows/headless `media_kit` 后端可用性限制，施工证据中需注明是否成功跑通。播放/视觉链路仍建议复核线程或用户用原始 m3u8 剧集做一次肉眼验收。
